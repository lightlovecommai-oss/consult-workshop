# 交接｜2026-09-02 路人頁重設計進行中・四頁已上線

> **這份接在 `_交接-2026-09-01-完整簡報.md` 之後**（那份的 §2 分析框架與 §4 四模組問答仍然有效，先讀它再讀這份）。
> 本 session（2026-09-01 晚～09-02）把執行清單 A 段大致做完、開了三支新頁面、
> 接通了後端會員旗標，**現在停在「路人第一屏重設計的畫布稿等使用者改」**。

## 0 · 開工

```bash
cd /Users/ivor/comconvertai/4-溝通健身房/consult-workshop && claude
```

設計/文案討論用 **Fable**（使用者會自己 `/model` 切）；照規格執行用預設 Sonnet。
**使用者是台灣人，所有輸出一律繁體中文。**

## 1 · 現在停在哪（最優先的事）

**路人第一屏（comconverttest 開場畫面）重設計稿在設計畫布上，使用者已開始在上面改字：**

- 畫布網址：`https://claude.ai/code/artifact/f488cf7d-be8a-4ae1-a6e6-08fe3b822609`
- 工作檔：`design-canvas/Main.dc.html` ＋ `canvas.json`（已進 repo；seeded 輸出在 .gitignore）
- 更新流程：改 Main.dc.html → 用 design skill 的 seed-canvas.mjs 重 seed → Artifact republish **同一個 URL**
  ⚠️ **republish 前一定先用 Artifact read + seed-canvas.mjs --extract 拉最新版**——使用者會直接在畫布上存檔，
  不拉最新版就 republish 會蓋掉他的修改
- **使用者改完/批准後 → 把定稿落回 `3-能力測驗/comconverttest/index.html` 的 `#s-intro`**，
  並把 s-explain／s-quiz／s-result 三屏的視覺一起對齊 30-規範（畫布只做了第一屏，後三屏還是舊視覺）

### 🔄 2026-09-02 14:30 補記：使用者已在畫布上改了五處（工作檔已同步他的版本）

1. hero 副句：「變現，是影響力自然**而然**的結果」→「變現，是影響力自然的結果」
2. 情境二標題：「1 對 1 深談」→「**1 對 1 顧問深談**」
3. 情境三標題：「1 對 N 演講／課程」→「**1 對 N 公眾演說**」
4. 測完你會拿到 #1：「哪塊**撐得住**」→「哪塊**用的好**」
5. 光頭自介改成：「**曾是廣告的專業人士，但講得很專業，卻無法好好表達自己**。學會這套方法後…」
   （對得上素材庫裡光頭開廣告公司的真實背景，是使用者自己寫的，照用）

使用者可能還會繼續改——落地前照上面 ⚠️ 的流程再拉一次最新版。

## 2 · 已定案・不要再問

- **行動優先結構**：hero 主張句(28px) → 降門檻膠囊(16題/約3分鐘/免費/不留資料) → CTA 進第一屏；情境/見證往下收 ← 使用者 2026-09-02 拍板
- **頁頭＝健檢為主**：logo＋「溝通影響力健檢」＋slogan「練好溝通，你就有影響力」當品牌繫帶 ← 同上拍板
- **整頁切溝通訴求，10–15% 輕帶變現**：hero 的「變現是自然而然的結果」＋能幫你第4條「甚至成交」＋王偉翔見證裡的「拓展客源」就是那 10-15%，不要再加
- **測驗改名「溝通影響力健檢」**（title/og/畫面標籤都改了，已上線）
- **測驗↔member.html 要 UI/語氣連貫**：同一套四階色層、同一個頭的結構、同一套語彙（溝通肌肉/健檢/秘訣）、主按鈕墨底 #4A1B0C
- **雅雯/阿澤見證換掉**：變現訴求對路人過度承諾（demo.html 先例）。已換成王偉翔＋匿名學員（都是📷逐字）
- **宋體不進介面**（30-規範硬規則）；層次感靠四階色層＋四級字＋一個28hero，不靠換字體
- **網址不動**：等 atpifit.com 網域接好（9/3 維護後）一次到位，現在改 repo 名是白工
- **私教學員新版另開 pro.html，dashboard.html 一行不動**（天麗在用）← 使用者拍板 fork
- **PayUni 走一頁商店＋每月手動請款**，手動勾「溝通健身房會員」欄開通

## 3 · 本 session 做完的（不要重做）

| 東西 | 狀態 |
|---|---|
| `member.html` v4（夥伴·練習·進步、單焦點練習、三個月驗收、體驗客/會員分層 `S.member`、體驗客秘訣按上線次數輪流） | ✅ 上線 |
| `pro.html`（私教學員科技版：路徑/體格/成果，L1-L8 顧問課、L9-L13 公眾演說真結構，關係軌跡讀真 `e.reaction`） | ✅ 上線 |
| `join.html`（99 轉換頁，`PAYUNI_URL` 常數留空、按鈕誠實顯示未接金流；`?paid=1` 看完成頁） | ✅ 上線 |
| `index.html` 路由（showcase 移出自動路由；超引力兩門課→pro.html；其餘開通→dashboard；其他人→member） | ✅ 上線 |
| `common.js` DIMS 四色全修成暖版＋textColor/soft；doCheckin/postCheckin 帶 share | ✅ 上線 |
| `Code.gs`：paidMember 旗標／setupChaoyinli（兩門課+開通欄+L1/L3真任務）／share 欄+gymPosts 端點 | ✅ **已部署**（curl 驗過 paidMember:true、gymPosts:ok） |
| comconverttest：改名健檢＋logo＋主張句＋4條能幫你溝通化＋光頭改寫＋阿澤→匿名學員 | ✅ 上線 |
| 兩 repo favicon/OG 分享圖（productkit 官方資產）；member/join/pro 補載 Noto Sans TC | ✅ 上線 |

## 4 · 未決・下一步（按優先序）

1. **畫布定稿 → 落回 comconverttest**（卡使用者改畫布）＋後三屏視覺對齊
2. **模組1剩兩條**：留資頁移到結果之後（`s-form`/`s-result` 對調）、LINE 分享降級——完整簡報 §10 B 段
3. **PayUni 接通**（卡使用者）：後台把交易通知 NotifyURL 先設 webhook.site 臨時網址 → 付一筆最小額 → 把收到的原始 payload 貼回來 → 照真格式寫 Code.gs 接收端 → `join.html` 的 `PAYUNI_URL` 填收款連結（`https://api.payuni.com.tw/api/uop/receive_info/2/1/U08115048/MW1vpXAy1zhtLtMhFvUZ3`）
4. L2(T)/L4(I) 任務池補齊（卡使用者給新逐字稿或口述確認）

## 5 · 踩過的坑・別重踩

- **L2 逐字稿（8/22 萃取）不能直接收任務池**——T 整組內容 8/30 才重構（7-11-4 退場），舊萃取的任務對不到現在的 T1/T2/T3。L1(A)/L3(P) 沒這個問題已收
- **「年收48萬→200萬」在見證庫查無來源**——見證數字用之前一律先對 `productkit/產品kit/見證庫-共筆.md`，查不到就不用；引言絕不代擬
- **收款連結的 Notify payload 格式未知**——網路文件是另一組「整合式支付頁 API」（AES-256-GCM），跟一頁商店可能不同套。**先 webhook.site 觀測真 payload 再寫程式，不要照 SDK 猜**
- **GAS 沒有 AES 加解密內建**——若最後要走 API 串接（非收款連結），這是平台限制要先面對
- **serve 會把 `.html?query` 301 吃掉參數**——本機測路由用乾淨網址或直接呼叫 `routeFor()` 驗邏輯
- **設計畫布是唯一協作介面**——不要出靜態 artifact 要使用者「留言」，他要每個板塊可直接點改
- **productkit 另一條線在寫**（現有 3 個未 commit 檔）——不要動 productkit
- 測試不要對正式 API 帶真 userId 亂按（會寫進正式試算表）；demo 模式用 `?demo=1`（member）＋`&member=0&visits=N` 看體驗客視角

## 6 · 怎麼驗收

```bash
cd /Users/ivor/comconvertai/4-溝通健身房/consult-workshop
for f in atpi-core.js common.js judgement.js; do node --check $f; done
curl -sL "<SHEET_API>?action=bootstrap&userId=U73cee7837af9e1103320de450ae5931c" | python3 -c "import json,sys; print(json.load(sys.stdin)['student'])"   # 要有 paidMember
curl -sL "<SHEET_API>?action=gymPosts&limit=5"    # 要回 {"status":"ok",...}
```

線上：`…/consult-workshop/member.html?demo=1`（會員視角）／`?demo=1&member=0`（體驗客）／
`…/comconverttest/`（健檢新開場）／`…/consult-workshop/join.html`（99 頁）

## 7 · 目前狀態

- consult-workshop `a411b8b`、comconverttest `d955d25`——兩邊工作區乾淨、全部已 push
- productkit `check.py` 全綠（本 session 沒動 productkit）
