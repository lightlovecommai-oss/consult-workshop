# 交接｜2026-09-01 四模組定案與執行清單

> 🔴 **本檔已被取代 → 讀 `_交接-2026-09-01-完整簡報.md`**
> 這份只有「結論清單」，沒有推理過程（三個問題的分析框架、被否決的選項、為什麼）。
> 接手的人遇到清單沒涵蓋的情況只能重猜——所以另寫了完整版。本檔留作對照。

> **這份是什麼**：老師 2026-09-01 一整天口述拍板的**全部產品決策** ＋ **待執行清單**。
> 判斷題今天已經做完，下一段是**照規格執行**。
> **前一份**＝`_健身房v3-SESSION交接.md`（架構仍可查，但 §1「三種 TA」已被本檔的「四種身分」取代）。

## 0 · 怎麼開下一個 session

```bash
cd /Users/ivor/comconvertai/4-溝通健身房/consult-workshop && claude
```

**Model＝Sonnet**（該目錄 `.claude/settings.json` 已寫 `claude-sonnet-5`，從這裡啟動自動生效）。
理由：接下來全部是照規格執行，判斷題今天拍完了。

⚠️ **`comconverttest` 的五條改動在另一個 repo**，要從那個目錄另開 session，而且**要等「簡易版測驗」那個 session 收工**。

### 🔴 開工前必讀的協作規則（今天差點出事）

> **productkit 同一時間只讓一個 session 寫。**
> 大腦層的檔互相引用太密，兩邊同時寫必然出事。
> 執行專案（consult-workshop／comconverttest／LP）可以平行，因為它們互不引用。

今天 productkit 一度有 **249 個檔異動、216 個改動全部沒 commit**（icon V3 改名 session ＋ 本 session 同時寫）。
兩邊剛好錯開沒撞到，但那是運氣。**動 productkit 前先 `git -C <productkit> status --short | wc -l`，數字很大就先問誰在動。**

---

## 1 · 四種身分（取代舊的「三種 TA」）

**路人**（還沒點過 app）→ **體驗客**（進來玩了、沒付 99）→ **會員**（付 99）→ **私教學員**（3.28 萬以上）

**⭐ 這四種身分 ＝ ATPI 門檻級聯的落地**（老師 2026-09-01 觀察，已進 01 字典）：

| 身分 | 水溫 | 我們要做的 |
|---|---|---|
| 路人 | 0–10° | **A 點火** |
| 體驗客 | 10–50° | **T 抬底線** |
| 會員 | 50–90° | **P 墊天花板** |
| 私教學員 | 90–100° | **I 閥門** |

用的是**水溫層不是能力層**，不違反 G6。意義：**我們用我們教的東西做我們的產品**，可直接當教學物。
**用途**：拿 12 小肌群當漏斗檢查表——對不上就是漏了一步。第一次跑就抓到 A3 與 T3 兩個缺口（已補）。

---

## 2 · 四個模組定案

### 模組 1 · 路人（A 點火）

| | |
|---|---|
| 流程 | 開場 → 說明 → 16 題 → **健檢結果** → **留資** → 進去看看 |
| 看到什麼 | 他自己（卡在哪）＋ **第一個秘訣**（完整、明天能用、**明示還有下一個**） |
| 鉤子 | 雙鉤：夥伴在練同一塊 ＋ 第二個秘訣在裡面免費 |
| 留存 | 沒有（他還沒身分）。「還有下一個」把留存變成轉換 |
| 出口 | **只有一個主出口＝進館**。分流已取消 |

**已拍板的改動**（⚠️ 在 `comconverttest`，等 Session A 收工）：
1. **留資頁移到結果之後**（`s-form` 與 `s-result` 對調）。代價是名單變少，老師已知並拍板
2. **刪掉 Calendly 出口**（`index.html:254, 695-697`）——付完 99 才有場邊教練進諮詢
3. **修「學員變現入口」撞牆**（`:261`）——`enrolled` 空值會被踢到 showcase，漏斗第一步就斷
4. **LINE 分享保留但降級成次要**（`:255-258`）——不能跟進館搶主 CTA
5. **開場訴求對齊「練溝通」不是「練變現」**（文案層可延後）

### 模組 2 · 體驗客（T 抬底線）

| | |
|---|---|
| 第一屏 | **夥伴** —— 團體感、陪伴感 |
| 內容 | **最卡那塊大肌肉的三個秘訣，按「上線次數」輪流**（不按日期） |
| 拿不到 | **回饋** —— 沒有解盤、記錄不進肌肉、沒有人回你 |
| 冷客戶 | **不降級**。秘訣發完繼續輪流、繼續能打卡 |
| 推力 | **他自己的時間軸**（不是跟別人比） |
| CTA | 主鈕 NT$99 ＋ 旁邊外框次鈕「免費講座」 |
| 問 99 的時機 | **每次接觸都問**。才 99 元，決策成本低 |

**⭐ 為什麼綁「上線次數」不綁日期**：多數人第一天打開後隔幾週才回來，按日期發他一定錯過。
**⭐ 為什麼三個輪流**：內容不枯竭，但也不新增——99 的界線不是靠鎖，是靠「本來就只有三個」。

**🔴 走陪伴，不走比較**（老師明確裁定）：
- ❌「小美這週練了 5 天，你只有 1 天」
- ✅「你上次來是 3 週前。那時最卡的是信任肌肉，今天還是——它不會自己長。」

### 模組 3 · 會員（P 墊天花板）

| | |
|---|---|
| 第一個 48 小時 | **場邊教練**私訊，陪他把溝通健檢看一遍 |
| 之後 | **每月覆盤一次，前三個月每月都跟** |
| 解盤呈現 | **一段話，不是報告**——頭像、名字、日期、他看完紀錄後說的那句 |
| 練哪塊 | 最弱 **2 塊大肌肉 ＝ 6 個小肌群**；**一個推薦 ＋ 換一個**，不攤開 |
| 三個月驗收 | **第一週 vs 最後一週的紀錄並排**，不給分數 |
| 升單 | 不用問的，**指出他資料裡的 P→I 缺口** |

**⭐ 兩條裁決如何並存**：用**最弱圈範圍**（2 塊大肌肉）、用**最有體驗排順序**（見效快的排前面）。
**⭐ 為什麼解盤要有臉**：99 買的不是分析，是「**有人**」。`report.html` 是顧問用的**輸入端**，會員這一側要**輸出端**。
**⭐ 關係軌跡的正確位置**：**驗收時的那一張圖**，不是日常功能。平常放著只會讓人每天看到自己「沒走完」。
**⭐ 三個月數值沒動怎麼辦**：誠實講——「練了 30 次反應沒變，可能練錯塊或方法要調，這種事看紀錄看不出來，要有人在旁邊看你怎麼講。」掩蓋比誠實更傷，而且這結論指向教練課。

### 模組 4 · 私教學員（I 閥門）

**⭐ 升級觸發用「回本」不用「進度」（老師 2026-09-01 修正）**：
原草案是「L8 結束後」，老師改成「**他賺到 3 萬就行**」。
3 萬 ≈ 3.28 萬課程費用回收——那一刻他心裡的話是「這套有用，而且已經把錢還我了」，
是最有信心投下一筆的時候。而且不必等課程走完，可能 L5 就發生。
⚠️ 這條要求系統**看得到他的成交回報**（那是層 3 才露出的欄位），所以成交回報不只是展示，是**升級的觸發器**。

**這一層的敵人是完課率，不是留存。** 買了不做 → 沒結果 → 不續會員、不推薦、不升 10.8 萬。

| | |
|---|---|
| 12 格怎麼開 | **不一次開**。L1→A（3格）／L2→T／L3→P／L4→I。永遠只面對 3 格 |
| 為什麼 | **節奏由課程給，不由他挑**——這正是多付 3.28 萬買到的（會員自己挑，學員有人排） |
| 甜蜜路徑 | **主：他紀錄裡成功率最高的序列 ＋ 場邊教練／顧問指定**；**輔：六種人那張表**（只當教學示意，不當個人結論） |
| 完課率 | **課／練兩條進度分開計**。「練」沒滿**只提示、照樣解鎖**（不硬鎖） |
| I2 差保障 | **練習對象分級**：先家人朋友 → 熟客 → 新客。⚠️ 老師補：但要帶「**市場一直在走，誰等你練好**」的推力，別讓分級變成拖延的藉口 |
| 升級 3.28→10.8 | **觸發＝他賺到 3 萬**（不是 L8 上完）。話術＝「你已經能一次講服一個人了。一次講服一百個，用的是同一套肌肉，只是換了場景。」訴求＝**讓你的影響力放大 N 倍** |
| 介面切換 | **混合，不全換**。「夥伴」頁保留溫暖版（他還是會員），只有課與數據頁是科技版。買課完成頁要有過場說明 |

---

## 3 · 用詞（暫定・尚未全面進字典）

**原則：保留身體的隱喻（肌肉／練／長／萎縮＝命門），拿掉設施的詞彙。**

| 舊 | 新 | 狀態 |
|---|---|---|
| 館／今天／我（三分頁） | **夥伴 · 練習 · 進步** | 🟡 只在 demo，未進字典 |
| 開練 | **練溝通** | 🟡 同上 |
| 動作／一招 | **一個秘訣** | 🟡 同上 |
| 體格 | **溝通肌肉** | 🟡 同上 |
| 體測 | **週回顧**（自評）／**溝通健檢**（教練帶） | 🟡 同上 |
| 一組 | 退場（直接說「今天這一件」） | 🟡 同上 |
| 館 | app＝**夥伴**／Skool＝**社群** | 🟡 同上 |
| 巡場教練 | **場邊教練**；動作＝**場邊回饋** | ✅ **已進 01 字典**（職稱條＋語彙表） |
| 讓他覺得有解（P 結果句） | **讓他要找你** | ✅ **已進 01 字典＋check.py** |

⚠️ **老師交代：用詞表先只改 demo，不要寫進 01 字典與 check.py——還可能會改。**
對照表以註解形式留在 `demo.html` 檔頭。

---

## 4 · 執行清單

### A · consult-workshop（可以自己做）

| # | 事 | 備註 |
|---|---|---|
| A1 | 把 `demo.html` 的四模組設計**落到真的 app**（`member.html` / `index.html` / `dashboard.html`） | demo 是規格，不是最終品 |
| A2 | `member.html` 的三分頁改成 **夥伴 · 練習 · 進步**，館排第一 | |
| A3 | **拿掉 `member.html` 的關係軌跡**（`arcHTML` :543-600），搬到層 3 | ⚠️ 家人／朋友煞車要跟著搬，不可拿掉 |
| A4 | 關係軌跡重建時**改讀對方反應**，不是「我練過這維」 | 現行 `arcHTML` 完全沒讀 `e.reaction` |
| A5 | 動態流分兩層：有打字＝大卡片可鼓勵／純點擊＝一行 | 沒人打字館裡也要會動 |
| A6 | 四色改用**暖版原版/深版**（`common.js:12-15` 的 T/P/I 是錯值） | 正確值見 `30-App介面規範` §5 |
| A7 | 全站對齊 `30-App介面規範`（無襯線、四級字、四階色、不做盒中盒、順序鎖 A→T→P→I） | |

### B · comconverttest（等 Session A 收工，另開 session）

模組 1 的五條，見上。清單也存在記憶 `comconverttest-pending-changes`。

### C · productkit（等 icon V3 那個 session 收工）

| # | 事 |
|---|---|
| C1 | `16§51`、`27§`（99＝巡場）、`20` 三處「巡場」→**場邊回饋** |
| C2 | 用詞表若老師拍板定案，寫進 01 字典《健身房語彙表》＋ `check.py` |
| C3 | 🟡 考慮把 `check.py` 的 `**粗體**` 從豁免拿掉——實測會多抓到 **10 處真漂移**，含 `03-產品體系總覽:133` 的舊價「套裝 5 萬」 |

### D · 卡使用者（老師親自做）

| # | 事 | 為什麼卡 |
|---|---|---|
| D1 | **Code.gs 貼進 GAS → 執行 `migrateV2` → 部署新版本** | 不做的話 v2 的資料層是斷的 |
| D2 | 接「分享到館裡」的死碼 | 前端有勾選、後端沒欄位，使用者以為分享了其實沒有。要動 Code.gs |
| D3 | **PayUni 定期定額申請**（幕後授權＋Token 交易） | 99 月費引擎完全靠它，有審核時間 |
| D4 | Namecheap 改 NS → Cloudflare（9/3 維護結束後） | 見記憶 `atpifit-domain-status` 的六步順序 |
| D5 | Skool 改名「溝通健身房」含 logo 與說明 | 老師：之後的事，不急 |
| D6 | 找兩則**溝通類見證**替換（如果覺得現有兩則不夠） | 現用的是截圖背書的王偉翔＋匿名學員，已是純溝通 |

---

## 5 · 🔴 不要做的事（防誤殺）

1. **不要現在設 GitHub Pages 的 Custom domain** —— 設下去 `github.io` 會 301 到還不存在的網域，測驗和 demo 會同時掛掉。等 NS 生效再說
2. **不要改 `atpi-core.js` 的 `name2` 欄** —— 歷史別名，字典明文保留
3. **不要動 `5-逐字稿-raw/`** —— 原始語料
4. **「言行一致」「結果思維」在心法欄是正確的**，只有在格名欄才要改
5. **家人／朋友煞車不可拿掉**
6. **workshopId 的「一階/二階/三階」字串不動** —— Sheet 資料鍵
7. **不做排行榜、名次、百分位** —— 違反「力量是為了給愛，不是廝殺」
8. **見證絕不代擬引言** —— 一律逐字取自 `見證庫-共筆.md`，優先用 📷 有截圖的

---

## 6 · 今天已完成（不要重做）

| 檔 | 做了什麼 |
|---|---|
| `demo.html`（新） | 四模組互動原型，一支手機從健檢走到付款，可反覆跑 |
| `productkit/1-手冊（內部）/30-App介面規範.md`（新） | 螢幕介面唯一真相：字體／四級字／四階色／圓角間距／ATPI 四色怎麼選／元件／數字露出政策／禁止清單 |
| `01-核心定義字典` | P 結果句「讓他要找你」＋P/I 分界句／四種身分×門檻級聯（新條）／場邊教練分層（職稱條＋語彙表） |
| `21`／`25` | 加指標指向 30 |
| `22` | P2/P3 顛倒修正（他證＝P3、視覺化Demo＝P2） |
| `check.py` | 攔「讓他覺得有解」等舊值 |
| `6-BNI通路/…README-講稿與Run-Down.md` | 結果句 ＋ 作廢的「缺一即零」 |
| `atpi-core.js`／`member.html` | `DIM_RESULT` 的 P ＋ 軌跡引導句 |
| `comconverttest/index.html` | `DIMS.desc` 四句結果句 |
| Cloudflare | atpifit.com DNS 全設好（4 筆 A ＋ 3 筆 CNAME，全部 DNS only；5 筆 MX 保留） |

## 7 · 怎麼驗收

```bash
cd /Users/ivor/comconvertai/4-溝通健身房/consult-workshop
for f in atpi-core.js common.js judgement.js; do node --check $f; done
npx -y serve -l 3100 .        # 開 localhost:3100/demo（serve 會 301 吃掉 .html）
cd ../../5-企劃與產品手冊/productkit && python3 check.py    # 要全綠
```

線上：`https://lightlovecommai-oss.github.io/consult-workshop/demo.html`

## 8 · 相關記憶

`gym-three-ta-layers-and-order`、`atpi-p-result-sentence`、`atpifit-domain-status`、
`comconverttest-pending-changes`、`multi-session-file-collision`、`bulk-edit-safety`、
`execution-recheck-dictionary-first`、`evidence-screenshot-over-recall`
